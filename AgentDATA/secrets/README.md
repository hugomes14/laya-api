# secrets

Guarde apenas placeholders ou instrucoes sobre segredos.

Segredos reais, tokens, credenciais e chaves privadas nao devem ser commitados.

Para MCP servers, use `mcp_credentials.example.json` como contrato de exemplo e crie arquivos locais nao versionados para valores reais.
