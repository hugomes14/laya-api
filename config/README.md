# Runtime configuration

The taxonomy and routing rules will be added here in later milestones. The
directory is mounted read-only by Docker Compose so configuration changes do
not require rebuilding the application image.
